export const useColumns = (keys: Array<any>, titleMap: Record<string, string>) => {
  return keys.filter(k => k in titleMap).map(key => {
    switch (key) {
      case value:

        break;

      default:
        break;
    }
  })
}
